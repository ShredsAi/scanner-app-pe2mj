<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty name="LOG_FILE" source="logging.file.name" defaultValue="scanner-discovery.log"/>
    <springProperty name="LOG_LEVEL" source="logging.level.ai.shreds" defaultValue="DEBUG"/>
    <springProperty name="ROOT_LOG_LEVEL" source="logging.level.root" defaultValue="INFO"/>
    <springProperty name="REDIS_LOG_LEVEL" source="logging.level.org.springframework.data.redis" defaultValue="INFO"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{ISO8601} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender with Rolling Policy -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
        <encoder>
            <pattern>%d{ISO8601} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Async Appender for better performance -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <includeCallerData>false</includeCallerData>
    </appender>

    <!-- Application specific loggers -->
    <logger name="ai.shreds" level="${LOG_LEVEL}" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>
    
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.springframework.data.redis" level="${REDIS_LOG_LEVEL}"/>
    <logger name="org.springframework.scheduling" level="INFO"/>
    <logger name="org.springframework.retry" level="INFO"/>
    <logger name="com.sun.jna" level="WARN"/>
    <logger name="net.java.dev.jna" level="WARN"/>

    <!-- Profile-specific configurations -->
    <springProfile name="dev">
        <logger name="ai.shreds" level="TRACE"/>
        <logger name="org.springframework.data.redis" level="DEBUG"/>
    </springProfile>

    <springProfile name="prod">
        <logger name="ai.shreds" level="INFO"/>
        <logger name="org.springframework.data.redis" level="WARN"/>
    </springProfile>

    <root level="${ROOT_LOG_LEVEL}">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_FILE"/>
    </root>
</configuration>